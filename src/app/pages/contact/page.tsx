import ContactClient from "./ContactClient";

export const metadata = {
  title: "Contact",
  description: "Get in touch with the ViaVanta administrative support team.",
};

export default function ContactPage() {
  return <ContactClient />;
}
